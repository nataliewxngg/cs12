package u1;

public class U1_Review_9 {
    public static void main(String[] args) {
        // a)
        // REPETITION: the process of repeating an act
        // RECURSION: a function which calls itself either directly or indirectly

        // b)
        // 1. There must be a base/degenerate case(s)
        // 2. There must be a way to reduce the problem so that it reaches the base
        // case(s)
        // 3. There must be a way of achieving the larger result from the smaller
        // elements

        // c)
        // StackOverFlowError = when a recursive function gets called
        // excessively/infinitely to the point where the space to store the
        // data/information exceeds the amount that can fit on the stack
        // usually occurs when recurisve functions infinitely call themselves (like an
        // infinite loop)
    }
}
